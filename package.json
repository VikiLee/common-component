{
  "name": "common-component",
  "version": "1.0.0-beta",
  "description": "common compoment for react",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && copyfiles -u 1 lib/**/*.css lib/**/*.svg lib/**/*.png dist",
    "build:types": "tsc --emitDeclarationOnly"
  },
  "keywords": [
    "react",
    "common",
    "component"
  ],
  "author": "weiji.li",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-env": "^7.4.5",
    "@babel/preset-react": "^7.0.0",
    "@babel/preset-typescript": "^7.3.3",
    "browser-env": "^3.2.6",
    "copyfiles": "^2.2.0",
    "typescript": "^3.5.1"
  },
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "files": [
    "dist",
    "lib"
  ]
}
